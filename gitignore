# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json

# Apps Script (clasp) — .clasp.json trae el Script ID de tu proyecto específico,
# no hace falta compartirlo. Si usas clasp, tu código real vive en apps-script/.
.clasp.json
.claspignore

# Node (por si en el futuro agregas tooling/build; el proyecto no lo necesita hoy)
node_modules/
npm-debug.log*

# Locales / temporales
*.local
.env
